function selectUser() {
    let userWindow = null;
    userWindow = window.open('/users/select',
                             `userSelect`,
                             'width=600,height=600,resizeable=no,location=no');
};